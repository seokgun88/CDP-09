<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.second.dao.Dao">
    <select id="select" parameterType="hashmap" resultType="com.test.second.object.CalendarObj">
        SELECT * from calendar where user_id=#{param1}
    </select>
    <insert id="insert">
    	INSERT INTO CALENDAR (USER_ID, SCHEDULE_ID, TITLE, START, END) VALUES (#{param1}, #{param2}, #{param3}, #{param4}, #{param5})
    </insert>
    <delete id="delete">
    	DELETE FROM CALENDAR WHERE USER_ID=#{param1} AND SCHEDULE_ID=#{param2} AND TITLE=#{param3} AND START=#{param4} AND END=#{param5}
    </delete>
</mapper>